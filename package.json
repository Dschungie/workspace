{
  "name": "nessha-workspace",
  "version": "0.1.0",
  "private": true,
  "description": "Isolated Workspace foundation for Nessha.",
  "engines": {
    "node": ">=22 <23"
  },
  "scripts": {
    "start": "node server.js",
    "test": "node --test test/*.test.js",
    "smoke": "node scripts/smoke.js",
    "audit:legacy-snapshot": "node scripts/legacy-workspace-inventory.js",
    "rehearse:legacy-chat-import": "node scripts/rehearse-legacy-chat-import.js"
  },
  "dependencies": {}
}
